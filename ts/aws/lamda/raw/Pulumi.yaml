name: serverless
runtime: nodejs
description: Basic example of a serverless AWS application
template:
  config:
    aws:region:
      description: The AWS region to deploy into
      default: ap-south-1
